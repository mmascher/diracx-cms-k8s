```
mysql -h dbod-cms-diracx-test.cern.ch -P 5569 -u admin -p < authdb.sql
mysql -h dbod-cms-diracx-test.cern.ch -P 5569 -u admin -p < schema.sql
mysql -h dbod-cms-diracx-test.cern.ch -P 5569 -u admin -p < schema_permissions.sql

mysql -h dbod-cms-diracx-test.cern.ch -P 5569 -u diracx -p
```
